# Dependencies
node_modules/

# Build artifacts
public/static/tailwind.css

# Wrangler / local dev (miniflare state, local D1)
.wrangler/
.dev.vars

# Production secret values (pushed to the Worker via scripts/set-secrets.sh)
.secrets.local

# Fiberplane local issue tracking
.fp/
