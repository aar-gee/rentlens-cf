# Dependencies
node_modules/

# Build artifacts
public/static/tailwind.css

# Wrangler / local dev (miniflare state, local D1)
.wrangler/
.dev.vars

# Secret values (prod + per-env, pushed via scripts/set-secrets.sh)
.secrets.local
.secrets.*.local

# Fiberplane local issue tracking
.fp/
